import { css } from '@emotion/core'

export default {
  techStack: css({
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    flexWrap: 'wrap',
    marginTop: '3.5rem',
  }),

  logo: css({
    marginBottom: '1.5rem',
    height: '3rem',
    opacity: 0.3,
    marginLeft: '1.0rem',
    marginRight: '1.0rem',
  }),
}
