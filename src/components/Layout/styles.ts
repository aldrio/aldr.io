import { css } from '@emotion/core'

export default {
  layout: css({
    minHeight: '100vh',
  }),

  content: css({
    margin: '0 auto',
    maxWidth: '40rem',
    padding: '0 1.0875rem 1.45rem',
  }),
}
